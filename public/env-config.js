// Placeholder — replaced at container startup by entrypoint.sh
window.__env__ = {
  // Defina via entrypoint/variáveis de ambiente em runtime.
  // Não versionar tokens/segredos neste arquivo.
  KOMMO_TOKEN: '',
  KOMMO_SUBDOMAIN: '',
};
